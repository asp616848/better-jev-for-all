"""Shared code between every benchmark harness under benchmarks/: the canonical
Item shape, the fixed-schema filter, model backends, and the evidence-bundle
writer. See benchmarks/README.md for the overall design.
"""
